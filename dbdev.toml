[package]
name = "ai_sql"
version = "1.0.0"
description = "AI inference SQL functions for Supabase — completions, classification, sentiment, extraction, translation, redaction, and embeddings. Self-deploys its own edge function via ai.deploy()."
homepage = "https://github.com/JesseVent/ai-sql"
repository = "https://github.com/JesseVent/ai-sql"
license = "MIT"
keywords = ["ai", "llm", "openai", "inference", "embeddings", "supabase"]

[package.metadata]
requires_supabase = true
